const Sequelize = require('sequelize');
const db = require('./_db');
const Product = db.models.

const Category = db.define('category', {
  name: {
    type: Sequelize.STRING,
    allowNull: false
  }
});

Category.findProductsByCatName(arr) {
  P
}

// what is P???? --FF

module.exports = Category;
